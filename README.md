eKool Africa demonstration application

Application has separate backend and frontend in respective  directories ("backend" and "frontend").
Dockerfiles for building are inside both directories.

This application uses built in H2 database.